# 0110 原型链补环境 欧冶-瑞数6

## 总结

1、 最终代码同时满足欧冶、深圳大学在医院、国家专利局

2、 一个重要的经验是：如果调试时，输出的信息很难判断是该补什么，此时应该把前面创建的对象监控起来，如：

```plain

[CALL] document.createElement("DIV")

TypeError: _$h7[_$jV[85]] is not a function

    at _$j7 (eval at _$kD (H:\Python\MyPythonProject\code\欧冶-瑞数
```

我们把 div 监控起来后很容易发现：

```plain
[CALL] document.createElement("DIV")
[GET] element<DIV>.getElementsByTagName undefined
```

我下面最终的代码里并没有监控 div，有需要可以让 AI 增加

## 补环境代码

### 1、 使用基础补环境框架

```plain
/************************************************************
 * 配置：双模式开关
 ************************************************************/
const DEBUG_MODE = true;   // 适合瑞数
// const DEBUG_MODE = false; // 适合知乎


/************************************************************
 * 模块 0：日志系统（logOnce）
 ************************************************************/
function v_log(...args) {
    console.log(...args);
}

const __seenLogKeys = new Set();

function logOnce(key, ...args) {
    if (__seenLogKeys.has(key)) return;
    __seenLogKeys.add(key);
    v_log(...args);
}


/************************************************************
 * 模块 1：安全 safeFunction（不修改 Function.prototype.toString）
 ************************************************************/
const nativeToString = Function.prototype.toString;
const nativeCode = " { [native code] }";

function safeFunction(func) {
    if (typeof func !== "function") return func;

    const name = func.name || "";
    const fake = `function ${name}()` + nativeCode;

    Object.defineProperty(func, "toString", {
        value: function () {
            return fake;
        },
        writable: false,
        enumerable: false,
        configurable: true
    });

    return func;
}


/************************************************************
 * 模块 2：Proxy 监控器（调试模式专用）
 ************************************************************/
function createHandler(objName, target) {
    return {
        get(target, prop, receiver) {
            const value = Reflect.get(target, prop, receiver);
            const propStr = typeof prop === "symbol" ? prop.toString() : String(prop);
            const key = objName + "." + propStr;

            logOnce("get:" + key, `[GET] ${objName}.${propStr}`, value);

            if (value === undefined) {
                logOnce("undefined:" + key, `⚠️ ${objName}.${propStr} → undefined`);
            }

            if (typeof value === "function") {
            // 如果是原生函数（如 setTimeout），通常需要 bind window
            // 如果是普通的属性访问，尽量保持其原始 bind 状态，或者判断其是否为构造函数
            // 简单处理：只针对 window 上的原生方法进行 bind
                if (target === windowInstance || target === globalThis) {
                    return value.bind(target);
                }
            }

            return value;
        },

        set(target, prop, value, receiver) {
            const propStr = typeof prop === "symbol" ? prop.toString() : String(prop);
            const key = objName + "." + propStr;

            logOnce("set:" + key, `[SET] ${objName}.${propStr} =`, value);
            return Reflect.set(target, prop, value, receiver);
        }
    };
}

function watch(obj, name = "anonymous") {
    return new Proxy(obj, createHandler(name, obj));
}


/************************************************************
 * 模块 3：Window 初始化（双模式核心）
 ************************************************************/
function Window() {
}

safeFunction(Window);

let windowInstance;

if (DEBUG_MODE) {
    // ⭐ 调试模式：window = globalThis（日志爆炸）
    windowInstance = globalThis;
    Object.setPrototypeOf(windowInstance, Window.prototype);
    v_log("🟩 调试模式：window = globalThis");
} else {
    // ⭐ 生产模式：window = new Window()（真实浏览器行为）
    windowInstance = new Window();
    globalThis.window = windowInstance;
    globalThis.self = windowInstance;
    globalThis.top = windowInstance;
    Object.setPrototypeOf(windowInstance, Window.prototype);
    v_log("🟦 生产模式：window = new Window()");
}

globalThis.window = windowInstance;
globalThis.self = windowInstance;
globalThis.top = windowInstance;


/************************************************************
 * 模块 4：Document
 ************************************************************/
function Document() {
}

safeFunction(Document);

Object.defineProperty(Document.prototype, Symbol.toStringTag, {
    value: "HTMLDocument"
});

const documentInstance = new Document();
windowInstance.document = documentInstance;
globalThis.document = documentInstance;


/************************************************************
 * 模块 5：Navigator
 ************************************************************/
function Navigator() {
}

safeFunction(Navigator);

Object.defineProperty(Navigator.prototype, Symbol.toStringTag, {
    value: "Navigator"
});

Object.defineProperty(Navigator.prototype, "userAgent", {
    get: () => "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Safari/537.36"
});

const navigatorInstance = new Navigator();
windowInstance.navigator = navigatorInstance;
globalThis.navigator = navigatorInstance;


/************************************************************
 * 模块 6：Screen
 ************************************************************/
function Screen() {
}

safeFunction(Screen);

Object.defineProperty(Screen.prototype, Symbol.toStringTag, {
    value: "Screen"
});

const screenInstance = new Screen();
windowInstance.screen = screenInstance;
globalThis.screen = screenInstance;


/************************************************************
 * 模块 7：History
 ************************************************************/
function History() {
}

safeFunction(History);

Object.defineProperty(History.prototype, Symbol.toStringTag, {
    value: "History"
});

const historyInstance = new History();
windowInstance.history = historyInstance;
globalThis.history = historyInstance;


/************************************************************
 * 模块 8：Location
 ************************************************************/
function Location() {
}

safeFunction(Location);

Location.prototype.toString = function () {
    return "https://zhihu.com/";
};

const locationInstance = new Location();
windowInstance.location = locationInstance;
globalThis.location = locationInstance;


/************************************************************
 * 模块 9：调试模式下挂 Proxy
 ************************************************************/
if (DEBUG_MODE) {
    v_log("🟩 调试模式：开始代理 window/document/navigator/screen/history/location");

    window = watch(windowInstance, "window");
    document = watch(documentInstance, "document");
    navigator = watch(navigatorInstance, "navigator");
    screen = watch(screenInstance, "screen");
    history = watch(historyInstance, "history");
    location = watch(locationInstance, "location");
}

```

### 2、调试跟踪

```plain
现在运行代码，报：
[GET] document.createElement undefined
⚠️ document.createElement → undefined
[GET] document.appendChild undefined
⚠️ document.appendChild → undefined
[GET] document.removeChild undefined
⚠️ document.removeChild → undefined
```

补 Document 核心 API

```plain
/************************************************************
 * 步骤 1：补全 Document 核心 API
 ************************************************************/

// 1. createElement：瑞数用它创建 meta, div, script 等
Document.prototype.createElement = function(tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.createElement("${tag}")`);
    
    // 创建一个基础元素对象
    const el = {
        tagName: tag,
        nodeName: tag,
        attributes: {},
        style: {},
        // 核心：元素也需要 get/setAttribute
        setAttribute: function(n, v) { this.attributes[n] = String(v); },
        getAttribute: function(n) { return this.attributes[n] || null; }
    };
    
    // 让元素方法也“安全”
    safeFunction(el.setAttribute);
    safeFunction(el.getAttribute);
    
    return el;
};

// 2. appendChild / removeChild：瑞数用于操作 DOM 树
Document.prototype.appendChild = function(child) {
    v_log(`[CALL] document.appendChild`);
    return child;
};

Document.prototype.removeChild = function(child) {
    v_log(`[CALL] document.removeChild`);
    return child;
};

// 3. 补全方法安全保护
safeFunction(Document.prototype.createElement);
safeFunction(Document.prototype.appendChild);
safeFunction(Document.prototype.removeChild);

/************************************************************
 * 步骤 2：补全 getElementsByTagName
 ************************************************************/

function HTMLCollection(list = []) {
    const instance = list;
    Object.setPrototypeOf(instance, HTMLCollection.prototype);
    instance.item = function(i) { return instance[i] || null; };
    safeFunction(instance.item);
    return instance;
}
Object.defineProperty(HTMLCollection.prototype, Symbol.toStringTag, {
    value: "HTMLCollection", configurable: true 
});
safeFunction(HTMLCollection);

Document.prototype.getElementsByTagName = function(tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.getElementsByTagName("${tag}")`);
    
    const res = [];
    // 特别注意：瑞数 6 必须要拿到 meta 标签
    if (tag === "META") {
        const meta = this.createElement("META");
        // 请在此处填入你网页里的真实 content
        meta.setAttribute("content", global.metacontent);
        res.push(meta);
    }
    
    return new HTMLCollection(res);
};
safeFunction(Document.prototype.getElementsByTagName);
```

继续运行，报

```plain
现在输出：

[CALL] document.createElement("DIV")

TypeError: _$h7[_$jV[85]] is not a function
    at _$j7 (eval at _$kD (H:\Python\MyPythonProject\code\欧冶-瑞数
```

瑞数执行了 `document.createElement("DIV")` 之后，立即对生成的 `div` 元素调用了某个方法（通常是 <code>**getElementsByTagName**</code> 或 <code>**appendChild**</code>），但由于你在 `createElement` 中返回的是一个纯字面量对象 `{}`，它没有继承 `Element` 或 `HTMLElement` 的原型方法，导致瑞数找不到对应函数而崩溃。

\*\*建立正确的元素原型链：\*\*为了让 `document.createElement` 产生的元素具有“祖传”方法，我们需要构建一个简单的原型链。

请在你的代码中，**模块 4 (Document)** 的位置，按照以下顺序补充：

```plain
/************************************************************
 * 步骤 2.1：建立元素原型链 (Node -> Element -> HTMLElement)
 ************************************************************/
function Node() {}
safeFunction(Node);

function Element() {}
Element.prototype = Object.create(Node.prototype);
safeFunction(Element);

function HTMLElement() {}
HTMLElement.prototype = Object.create(Element.prototype);
safeFunction(HTMLElement);

// 在原型上统一挂载瑞数常用的方法
Element.prototype.getElementsByTagName = function(tag) {
    v_log(`[CALL] <${this.tagName}>.getElementsByTagName("${tag}")`);
    return new HTMLCollection([]); // 返回我们在上一步定义的 HTMLCollection
};
Element.prototype.setAttribute = function(n, v) { this[n] = String(v); };
Element.prototype.getAttribute = function(n) { return this[n] || null; };

safeFunction(Element.prototype.getElementsByTagName);
safeFunction(Element.prototype.setAttribute);
safeFunction(Element.prototype.getAttribute);
```

**修改 **<code>**createElement**</code>** 关联原型**

修改你之前的 `createElement`，让它返回的对象继承自 `HTMLElement`。

```plain
/************************************************************
 * 步骤 2.2：修正 createElement
 ************************************************************/
Document.prototype.createElement = function(tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.createElement("${tag}")`);
    
    // 创建一个继承自 HTMLElement 的实例
    const el = Object.create(HTMLElement.prototype);
    
    el.tagName = tag;
    el.nodeName = tag;
    el.style = {};
    el.attributes = {};
    
    return el;
};
safeFunction(Document.prototype.createElement);
```

现在报

```plain
H:\Python\MyPythonProject\code\欧冶-瑞数6\01_env_copilot.js:160
    return new HTMLCollection([]); // 返回我们在上一步定义的 HTMLCollection
    ^
ReferenceError: HTMLCollection is not defined
```

这个错误是因为我们在代码中调用了 `new HTMLCollection([])`，但在当前的执行作用域内，`HTMLCollection` 这个构造函数还没有被定义，或者定义的顺序在调用之后。

在补环境时，**定义的顺序非常重要**。我们需要先定义 `HTMLCollection`，再定义 `Element.prototype`。

定义 HTMLCollection (放在最前面)

```plain
/************************************************************
 * 步骤 3.1：HTMLCollection 构造函数
 ************************************************************/
function HTMLCollection(list = []) {
    // 瑞数会检测 item 方法和 length 属性
    const instance = list;
    Object.setPrototypeOf(instance, HTMLCollection.prototype);
    
    instance.item = function(i) {
        return instance[i] || null;
    };
    safeFunction(instance.item);

    // 某些版本还会检测 namedItem
    instance.namedItem = function(name) {
        for (let el of instance) {
            if (el.id === name || (el.attributes && el.attributes.name === name)) return el;
        }
        return null;
    };
    safeFunction(instance.namedItem);

    return instance;
}

// 必须设置这个，瑞数会 toString.call 检测
Object.defineProperty(HTMLCollection.prototype, Symbol.toStringTag, {
    value: "HTMLCollection",
    configurable: true
});
safeFunction(HTMLCollection);

// 挂载到 window
windowInstance.HTMLCollection = HTMLCollection;
```

定义元素原型链 (Node -> Element -> HTMLElement)

```plain
function Node() {}
safeFunction(Node);

function Element() {}
Element.prototype = Object.create(Node.prototype);
safeFunction(Element);

function HTMLElement() {}
HTMLElement.prototype = Object.create(Element.prototype);
safeFunction(HTMLElement);

// 核心：在 Element 原型上实现 getElementsByTagName
Element.prototype.getElementsByTagName = function(tag) {
    v_log(`[CALL] <${this.tagName}>.getElementsByTagName("${tag}")`);
    // 元素调用此方法，通常返回空集合即可
    return new HTMLCollection([]); 
};
safeFunction(Element.prototype.getElementsByTagName);
```

再完善 Document 的方法

```plain
Document.prototype.createElement = function(tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.createElement("${tag}")`);
    const el = Object.create(HTMLElement.prototype);
    el.tagName = tag;
    el.nodeName = tag;
    el.style = {};
    el.attributes = {};
    return el;
};
safeFunction(Document.prototype.createElement);

Document.prototype.getElementsByTagName = function(tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.getElementsByTagName("${tag}")`);
    
    let res = [];
    // 瑞数 6 关键点：Meta 标签
    if (tag === "META") {
        const meta = this.createElement("META");
        // 这里必须填入真实的 content
        meta.setAttribute("content", "YOUR_REAL_CONTENT"); 
        res.push(meta);
    }
    return new HTMLCollection(res);
};
safeFunction(Document.prototype.getElementsByTagName);
```

现在报

```plain
[GET] document.getElementsByTagName undefined
⚠️ document.getElementsByTagName → undefined
```

这个报错 `document.getElementsByTagName → undefined` 说明虽然你在 `Document.prototype` 上定义了方法，但 <code>**documentInstance**</code>**（即 **<code>**document**</code>** 对象）** 并没有成功继承到这个方法

修正继承关系与方法挂载  ，确保原型链正确挂载：确保 `documentInstance` 的原型指向 `Document.prototype`。

```plain
/************************************************************
 * 步骤 4.1：Document 实例初始化
 ************************************************************/
function Document() {}
safeFunction(Document);

// 确保原型上有方法
Document.prototype.getElementsByTagName = function(tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.getElementsByTagName("${tag}")`);
    let res = [];
    if (tag === "META") {
        const meta = this.createElement("META");
        meta.setAttribute("content", "YOUR_REAL_CONTENT"); // 必填
        res.push(meta);
    }
    return new HTMLCollection(res);
};
safeFunction(Document.prototype.getElementsByTagName);

// 关键：创建实例并指定原型
const documentInstance = new Document();
Object.setPrototypeOf(documentInstance, Document.prototype); 

// 挂载到全局
windowInstance.document = documentInstance;
globalThis.document = documentInstance;
```

***

**继续测试**

```plain
[GET] window.addEventListener undefined

⚠️ window.addEventListener → undefined

[GET] window.attachEvent undefined

⚠️ window.attachEvent → undefined
```

***

补全事件系统（EventTarget）：**我们需要模拟一个基础的 **<code>**EventTarget**</code>** 类，并让 **<code>**Window**</code>** 和 **<code>**Node**</code>** 继承它。这样 **<code>**window**</code>** 和 **<code>**document**</code>** 就自然拥有了 **<code>**addEventListener**</code>** 方法。**

首先定义 EventTarget，**将这段代码放在 **<code>**Window**</code>** 定义之前**

```plain
/************************************************************
 * 步骤 5.1：EventTarget (所有事件宿主的基类)
 ************************************************************/
function EventTarget() { }

EventTarget.prototype.addEventListener = function(type, listener) {
    v_log(`[CALL] addEventListener("${type}")`);
    // 最小化实现：瑞数初期主要是注册，不一定立即触发
};
EventTarget.prototype.removeEventListener = function(type, listener) {
    v_log(`[CALL] removeEventListener("${type}")`);
};
EventTarget.prototype.dispatchEvent = function(event) {
    v_log(`[CALL] dispatchEvent("${event.type}")`);
    return true;
};

safeFunction(EventTarget);
safeFunction(EventTarget.prototype.addEventListener);
safeFunction(EventTarget.prototype.removeEventListener);
safeFunction(EventTarget.prototype.dispatchEvent);
```

\*\*接着 让 Window 继承 EventTarget  ， 修改你的 模块 3 (Window)，并 关联到实例  \*\*

```plain
/************************************************************
 * 步骤 5.2：Window 初始化（增强版）
 ************************************************************/
function Window() {
    // 瑞数有时会检测 window 对象上是否有某些私有属性
}

// 建立继承关系：Window -> EventTarget
Window.prototype = Object.create(EventTarget.prototype);
Window.prototype.constructor = Window;

// 补充 attachEvent (IE 遗留，瑞数必检)
Window.prototype.attachEvent = function(type, handler) {
    v_log(`[CALL] window.attachEvent("${type}")`);
    const eventName = type.startsWith("on") ? type.slice(2) : type;
    return this.addEventListener(eventName, handler);
};
safeFunction(Window.prototype.attachEvent);
safeFunction(Window);

let windowInstance;

if (DEBUG_MODE) {
    // 调试模式：window 就是 globalThis
    windowInstance = globalThis;
    
    // 关键：修改 globalThis 的原型链，使其继承 Window.prototype 和 EventTarget.prototype
    Object.setPrototypeOf(windowInstance, Window.prototype);
    
    // 如果 setPrototypeOf 不生效（某些 Node 环境下），手动补丁：
    if (!windowInstance.addEventListener) {
        windowInstance.addEventListener = Window.prototype.addEventListener.bind(windowInstance);
        windowInstance.removeEventListener = Window.prototype.removeEventListener.bind(windowInstance);
        windowInstance.dispatchEvent = Window.prototype.dispatchEvent.bind(windowInstance);
        windowInstance.attachEvent = Window.prototype.attachEvent.bind(windowInstance);
    }
    
    v_log("🟩 调试模式：window = globalThis，已挂载事件系统");
} else {
    // 生产模式
    windowInstance = new Window();
    Object.setPrototypeOf(windowInstance, Window.prototype);
    v_log("🟦 生产模式：window = new Window()");
}

// 统一映射
globalThis.window = windowInstance;
globalThis.self = windowInstance;
globalThis.top = windowInstance;
```

继续运行，报

```plain
[GET] document.getElementById undefined
⚠️ document.getElementById → undefined
```

我们需要在 `Document.prototype` 上补充最常用的查询接口  ， 在 **模块 4** 或 `Document` 定义区域添加以下方法：

```plain
/************************************************************
 * 步骤 6.1：Document 查询 API 补全
 ************************************************************/

// 1. getElementById: 瑞数最常用的查询方式
Document.prototype.getElementById = function(id) {
    v_log(`[CALL] document.getElementById("${id}")`);
    // 某些情况下，瑞数会找特定的 ID。如果后续报错，我们再根据 ID 返回 mock 对象
    return null; 
};

// 2. querySelector: 现代浏览器常用接口
Document.prototype.querySelector = function(selector) {
    v_log(`[CALL] document.querySelector("${selector}")`);
    return null;
};

// 3. querySelectorAll: 返回一个静态的 NodeList
Document.prototype.querySelectorAll = function(selector) {
    v_log(`[CALL] document.querySelectorAll("${selector}")`);
    // 借用之前定义的 HTMLCollection 或返回一个空数组，并模拟 item 方法
    const res = [];
    res.item = function(i) { return res[i] || null; };
    return res;
};

// 4. 别忘了 getElementsByClassName
Document.prototype.getElementsByClassName = function(className) {
    v_log(`[CALL] document.getElementsByClassName("${className}")`);
    return new HTMLCollection([]);
};

// 安全化处理
safeFunction(Document.prototype.getElementById);
safeFunction(Document.prototype.querySelector);
safeFunction(Document.prototype.querySelectorAll);
safeFunction(Document.prototype.getElementsByClassName);
```

检查 documentInstance 的继承，确保你的 `documentInstance` 已经链接到了这个原型

```plain
// 在创建实例后执行
Object.setPrototypeOf(documentInstance, Document.prototype);
```

现在报 location 缺少 port、protocol、host 等等

```plain
// --- Location ---
function Location() {
}

safeFunction(Location);
Location.prototype.toString = function () {
    return "https://www.ouyeel.com/steel";
};
safeFunction(Location.prototype.toString);

Object.defineProperties(Location.prototype, {
    href: {
        get() {
            return "https://www.ouyeel.com/steel";
        }, configurable: true
    },
    protocol: {
        get() {
            return "https:";
        }, configurable: true
    },
    host: {
        get() {
            return "www.ouyeel.com";
        }, configurable: true
    },
    hostname: {
        get() {
            return "www.ouyeel.com";
        }, configurable: true
    },
    port: {
        get() {
            return "";
        }, configurable: true
    },
    origin: {
        get() {
            return "https://www.ouyeel.com";
        }, configurable: true
    },
    pathname: {
        get() {
            return "/";
        }, configurable: true
    },
    search: {
        get() {
            return "";
        }, configurable: true
    },
    hash: {
        get() {
            return "";
        }, configurable: true
    }
});
const locationInstance = new Location();
windowInstance.location = locationInstance;
globalThis.location = locationInstance;
```

再补充<font style="color:rgb(31, 31, 31);background-color:rgb(233, 238, 246);">\[GET] window.ActiveXObject undefined</font>

```plain
/************************************************************
 * 步骤 7.1：环境指纹补全 (ActiveXObject)
 ************************************************************/

// 在浏览器中，ActiveXObject 是探测是否为 IE 的关键
// 对于 Chrome 环境，它应该存在于 window 上，但值为 undefined
Object.defineProperty(windowInstance, "ActiveXObject", {
    value: undefined,
    writable: true,
    enumerable: false, // 现代浏览器通常不可枚举
    configurable: true
});

v_log("✅ 已补全 window.ActiveXObject (undefined)");
```

此时已经能输出 304 位长度的 cookie，满足 python 使用需要，

### 3、完整版

现在让 ai 将所有代码重新整理一下：

```plain
/************************************************************
 * 1. 基础工具与安全模块
 ************************************************************/
const DEBUG_MODE = true; 

function v_log(...args) { console.log(...args); }

const __seenLogKeys = new Set();
function logOnce(key, ...args) {
    if (__seenLogKeys.has(key)) return;
    __seenLogKeys.add(key);
    v_log(...args);
}

const nativeToString = Function.prototype.toString;
const nativeCode = " { [native code] }";

function safeFunction(func) {
    if (typeof func !== "function") return func;
    const name = func.name || "";
    const fake = `function ${name}()` + nativeCode;
    Object.defineProperty(func, "toString", {
        value: function () { return fake; },
        writable: false, enumerable: false, configurable: true
    });
    return func;
}

/************************************************************
 * 2. 事件系统基类 (EventTarget)
 ************************************************************/
function EventTarget() {
    this._listeners = {};
}
EventTarget.prototype.addEventListener = function (type, listener) {
    v_log(`[CALL] addEventListener("${type}")`);
};
EventTarget.prototype.removeEventListener = function (type, listener) {
    v_log(`[CALL] removeEventListener("${type}")`);
};
EventTarget.prototype.dispatchEvent = function (event) {
    v_log(`[CALL] dispatchEvent("${event.type}")`);
    return true;
};
safeFunction(EventTarget);
safeFunction(EventTarget.prototype.addEventListener);
safeFunction(EventTarget.prototype.removeEventListener);
safeFunction(EventTarget.prototype.dispatchEvent);

/************************************************************
 * 3. DOM 核心原型链 (Node -> Element -> HTMLElement)
 ************************************************************/

// --- HTMLCollection ---
function HTMLCollection(list = []) {
    const instance = list;
    Object.setPrototypeOf(instance, HTMLCollection.prototype);
    instance.item = function (i) { return instance[i] || null; };
    instance.namedItem = function (name) {
        for (let el of instance) {
            if (el.id === name || (el.attributes && el.attributes.name === name)) return el;
        }
        return null;
    };
    safeFunction(instance.item);
    safeFunction(instance.namedItem);
    return instance;
}
Object.defineProperty(HTMLCollection.prototype, Symbol.toStringTag, {
    value: "HTMLCollection", configurable: true
});
safeFunction(HTMLCollection);

// --- Node ---
function Node() {}
safeFunction(Node);

// --- Element ---
function Element() {}
Element.prototype = Object.create(Node.prototype);
Element.prototype.setAttribute = function (n, v) {
    if (!this.attributes) this.attributes = {};
    this.attributes[n] = String(v);
    if (n === 'id') this.id = v;
};
Element.prototype.getAttribute = function (n) {
    return this.attributes ? (this.attributes[n] || null) : null;
};
Element.prototype.getElementsByTagName = function (tag) {
    v_log(`[CALL] <${this.tagName}>.getElementsByTagName("${tag}")`);
    return new HTMLCollection([]);
};
safeFunction(Element);
safeFunction(Element.prototype.setAttribute);
safeFunction(Element.prototype.getAttribute);
safeFunction(Element.prototype.getElementsByTagName);

// --- HTMLElement ---
function HTMLElement() {}
HTMLElement.prototype = Object.create(Element.prototype);
safeFunction(HTMLElement);

/************************************************************
 * 4. Document 模块
 ************************************************************/
function Document() {}
Document.prototype = Object.create(Node.prototype);
Object.defineProperty(Document.prototype, Symbol.toStringTag, {
    value: "HTMLDocument"
});

Document.prototype.createElement = function (tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.createElement("${tag}")`);
    const el = Object.create(HTMLElement.prototype);
    el.tagName = tag;
    el.nodeName = tag;
    el.style = {};
    el.attributes = {};
    return el;
};

Document.prototype.getElementsByTagName = function (tagName) {
    const tag = String(tagName).toUpperCase();
    v_log(`[CALL] document.getElementsByTagName("${tag}")`);
    let res = [];
    if (tag === "META") {
        const meta = this.createElement("META");
        // 瑞数 6 核心补环境点：必须返回真实的 content
        meta.setAttribute("content", "YOUR_REAL_META_CONTENT_HERE"); 
        res.push(meta);
    }
    return new HTMLCollection(res);
};

Document.prototype.getElementById = function (id) {
    v_log(`[CALL] document.getElementById("${id}")`);
    return null;
};

Document.prototype.querySelector = function (selector) {
    v_log(`[CALL] document.querySelector("${selector}")`);
    return null;
};

Document.prototype.querySelectorAll = function (selector) {
    v_log(`[CALL] document.querySelectorAll("${selector}")`);
    const res = [];
    res.item = function (i) { return res[i] || null; };
    return res;
};

Document.prototype.getElementsByClassName = function (className) {
    v_log(`[CALL] document.getElementsByClassName("${className}")`);
    return new HTMLCollection([]);
};

Document.prototype.appendChild = function (child) {
    v_log(`[CALL] document.appendChild`);
    return child;
};

Document.prototype.removeChild = function (child) {
    v_log(`[CALL] document.removeChild`);
    return child;
};

safeFunction(Document);
safeFunction(Document.prototype.createElement);
safeFunction(Document.prototype.getElementsByTagName);
safeFunction(Document.prototype.getElementById);
safeFunction(Document.prototype.querySelector);
safeFunction(Document.prototype.querySelectorAll);
safeFunction(Document.prototype.getElementsByClassName);
safeFunction(Document.prototype.appendChild);
safeFunction(Document.prototype.removeChild);

const documentInstance = new Document();
Object.setPrototypeOf(documentInstance, Document.prototype);

/************************************************************
 * 5. Window 及 BOM 对象
 ************************************************************/
function Window() {}
Window.prototype = Object.create(EventTarget.prototype);
Window.prototype.constructor = Window;

Window.prototype.attachEvent = function (type, handler) {
    v_log(`[CALL] window.attachEvent("${type}")`);
    const eventName = type.startsWith("on") ? type.slice(2) : type;
    return this.addEventListener(eventName, handler);
};
safeFunction(Window.prototype.attachEvent);
safeFunction(Window);

let windowInstance;
if (DEBUG_MODE) {
    windowInstance = globalThis;
    Object.setPrototypeOf(windowInstance, Window.prototype);
    // 手动补齐 global 缺失的事件方法
    if (!windowInstance.addEventListener) {
        windowInstance.addEventListener = Window.prototype.addEventListener.bind(windowInstance);
        windowInstance.removeEventListener = Window.prototype.removeEventListener.bind(windowInstance);
        windowInstance.dispatchEvent = Window.prototype.dispatchEvent.bind(windowInstance);
        windowInstance.attachEvent = Window.prototype.attachEvent.bind(windowInstance);
    }
    v_log("🟩 调试模式：window = globalThis");
} else {
    windowInstance = new Window();
    Object.setPrototypeOf(windowInstance, Window.prototype);
    v_log("🟦 生产模式：window = new Window()");
}

// 全局映射
globalThis.window = windowInstance;
globalThis.self = windowInstance;
globalThis.top = windowInstance;
globalThis.document = documentInstance;
windowInstance.document = documentInstance;
windowInstance.HTMLCollection = HTMLCollection;

// --- Navigator ---
function Navigator() {}
safeFunction(Navigator);
Object.defineProperty(Navigator.prototype, Symbol.toStringTag, { value: "Navigator" });
Object.defineProperty(Navigator.prototype, "userAgent", {
    get: () => "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Safari/537.36"
});
const navigatorInstance = new Navigator();
windowInstance.navigator = navigatorInstance;
globalThis.navigator = navigatorInstance;

// --- Screen ---
function Screen() {}
safeFunction(Screen);
Object.defineProperty(Screen.prototype, Symbol.toStringTag, { value: "Screen" });
const screenInstance = new Screen();
windowInstance.screen = screenInstance;
globalThis.screen = screenInstance;

// --- History ---
function History() {}
safeFunction(History);
Object.defineProperty(History.prototype, Symbol.toStringTag, { value: "History" });
const historyInstance = new History();
windowInstance.history = historyInstance;
globalThis.history = historyInstance;

// --- Location ---
function Location() {}
safeFunction(Location);
Location.prototype.toString = function () { return "https://www.ouyeel.com/steel"; };
safeFunction(Location.prototype.toString);

Object.defineProperties(Location.prototype, {
    href: { get() { return "https://www.ouyeel.com/steel"; }, configurable: true },
    protocol: { get() { return "https:"; }, configurable: true },
    host: { get() { return "www.ouyeel.com"; }, configurable: true },
    hostname: { get() { return "www.ouyeel.com"; }, configurable: true },
    port: { get() { return ""; }, configurable: true },
    origin: { get() { return "https://www.ouyeel.com"; }, configurable: true },
    pathname: { get() { return "/"; }, configurable: true },
    search: { get() { return ""; }, configurable: true },
    hash: { get() { return ""; }, configurable: true }
});
const locationInstance = new Location();
windowInstance.location = locationInstance;
globalThis.location = locationInstance;

// --- 特殊环境指纹 ---
Object.defineProperty(windowInstance, "ActiveXObject", {
    value: undefined, writable: true, enumerable: false, configurable: true
});

/************************************************************
 * 6. Proxy 监控器 (调试模式专用)
 ************************************************************/
function createHandler(objName, targetObj) {
    return {
        get(target, prop, receiver) {
            const value = Reflect.get(target, prop, receiver);
            const propStr = typeof prop === "symbol" ? prop.toString() : String(prop);
            const key = objName + "." + propStr;

            logOnce("get:" + key, `[GET] ${objName}.${propStr}`, value);
            if (value === undefined) {
                logOnce("undefined:" + key, `⚠️ ${objName}.${propStr} → undefined`);
            }
            if (typeof value === "function") {
                return value.bind(target);
            }
            return value;
        },
        set(target, prop, value, receiver) {
            const propStr = typeof prop === "symbol" ? prop.toString() : String(prop);
            v_log(`[SET] ${objName}.${propStr} =`, value);
            return Reflect.set(target, prop, value, receiver);
        }
    };
}

if (DEBUG_MODE) {
    v_log("🟩 调试模式：开启 Proxy 监控");
    window = new Proxy(windowInstance, createHandler("window", windowInstance));
    document = new Proxy(documentInstance, createHandler("document", documentInstance));
    navigator = new Proxy(navigatorInstance, createHandler("navigator", navigatorInstance));
    location = new Proxy(locationInstance, createHandler("location", locationInstance));
}

v_log("🚀 环境初始化完毕");
```

### 4、基于 google gemini 优化的版本

```python
/************************************************************
 * 模块 0：日志去重（方案 1：同一属性只打印一次）
 ************************************************************/

function v_log(...args) {
    console.log(...args);
}

const __seenLogKeys = new Set();

function logOnce(key, ...args) {
    if (__seenLogKeys.has(key)) return;
    __seenLogKeys.add(key);
    v_log(...args);
}

/************************************************************
 * 模块 1：核心基础设施（Proxy + safeFunction + Window）
 ************************************************************/

function safeFunction(func) {
    if (!Function.prototype.$call) Function.prototype.$call = Function.prototype.call;
    if (!Function.prototype.$toString) Function.prototype.$toString = Function.prototype.toString;

    const sym = Symbol("native_code");

    const fakeToString = function () {
        if (typeof this === "function" && this[sym]) {
            return this[sym];
        }
        return Function.prototype.$toString.call(this);
    };

    Object.defineProperty(Function.prototype, "toString", {
        value: fakeToString,
        writable: true,
        configurable: true
    });

    if (typeof func === "function") {
        const name = func.name || "";
        Object.defineProperty(func, sym, {
            value: `function ${name}() { [native code] }`
        });
    }

    return func;
}

function createHandler(objName, target) {
    const SILENT_PROPS = new Set([
        "isNaN", "isFinite", "parseInt", "parseFloat", "encodeURI",
        "encodeURIComponent", "decodeURI", "decodeURIComponent",
        "JSON", "Math", "Reflect", "Atomics", "undefined", "Infinity",
        "-Infinity", "NaN", "console", "Uint8Array", "ArrayBuffer",
        "eval", "arguments", "caller", "callee"
    ]);

    return {
        get(target, prop, receiver) {
            // ⭐ 修复：先判断 prop 是否为 Symbol，避免 isNaN 报错
            if (typeof prop !== "symbol" && !(prop in target) && !isNaN(prop)) {
                return undefined;
            }

            const value = Reflect.get(target, prop, receiver);
            const propStr = typeof prop === "symbol" ? prop.toString() : String(prop);
            const key = objName + "." + propStr;

            if (!SILENT_PROPS.has(propStr)) {
                logOnce("get:" + key, "方法:", "get", "对象:", objName, "属性:", propStr, "属性值:", value, "类型:", typeof value);
            }

            if (value === undefined) {
                logOnce("undefined:" + key, "⚠️ 警告：", objName + "." + propStr, "返回 undefined");
            }

            if (typeof value === "function") {
                return value.bind(target);
            }

            return value;
        },

        set(target, prop, value, receiver) {
            const propStr = typeof prop === "symbol" ? prop.toString() : String(prop);
            const key = objName + "." + propStr;
            logOnce("set:" + key, "方法:", "set", "对象:", objName, "属性:", propStr, "新值:", value);
            return Reflect.set(target, prop, value, receiver);
        }
    };
}

function watch(obj, name = "anonymous") {
    return new Proxy(obj, createHandler(name, obj));
}

function setProxyArr(arr) {
    for (const name of arr) {
        let obj = globalThis[name];
        if (obj === undefined) {
            v_log("警告: 对象不存在，创建空对象:", name);
            obj = {};
            globalThis[name] = obj;
        }
        globalThis[name] = new Proxy(obj, createHandler(name, obj));
        v_log("✅ 成功代理对象:", name);
    }
}

/************************************************************
 * 模块 2： Window 原型链调整
 ************************************************************/

function Window() {
    EventTarget.call(this);
}

Object.setPrototypeOf(Window.prototype, EventTarget.prototype);

Object.defineProperty(Window.prototype, Symbol.toStringTag, {
    value: "Window",
    configurable: true
});

Window.prototype.attachEvent = function attachEvent(type, listener) {
    v_log(`[Window] 调用遗留方法 attachEvent: ${type}`);
    return this.addEventListener(type.replace(/^on/, ''), listener);
};

Window.prototype.scrollTo = function scrollTo(x, y) {
    v_log(`[Window] scrollTo(${x}, ${y})`);
};
Window.prototype.getComputedStyle = function getComputedStyle(el) {
    v_log(`[Window] getComputedStyle`);
    return watch({}, "ComputedStyle");
};

safeFunction(Window);
safeFunction(Window.prototype.attachEvent);
safeFunction(Window.prototype.scrollTo);
safeFunction(Window.prototype.getComputedStyle);

// 按照 Location 模式：多重挂载与全局关联
window = globalThis;
Object.setPrototypeOf(window, Window.prototype);

globalThis.Window = Window; // 暴露大写构造函数
self = window;
top = window;
parent = window;

// ⭐ 补齐 Chrome 风格的 window.ActiveXObject
Object.defineProperty(window, "ActiveXObject", {
    value: undefined,
    writable: true,
    enumerable: false,
    configurable: true
});
/************************************************************
 * 模块 3：DOM 核心基础设施 (EventTarget -> Node -> Element)+  document
 ************************************************************/

const _listeners = Symbol("listeners");

function EventTarget() {
}

safeFunction(EventTarget);

EventTarget.prototype.addEventListener = function addEventListener(type, listener) {
    if (!this[_listeners]) {
        Object.defineProperty(this, _listeners, {value: {}, enumerable: false});
    }
    if (!this[_listeners][type]) this[_listeners][type] = [];
    this[_listeners][type].push(listener);
    v_log(`[EventTarget] ${this.constructor.name} 注册事件: ${type}`);
};
EventTarget.prototype.removeEventListener = function removeEventListener(type, listener) {
    v_log(`[EventTarget] ${this.constructor.name} 移除事件: ${type}`);
};
EventTarget.prototype.dispatchEvent = function dispatchEvent(event) {
    const type = event.type;
    if (this[_listeners] && this[_listeners][type]) {
        this[_listeners][type].forEach(fn => fn.call(this, event));
    }
    return true;
};
safeFunction(EventTarget.prototype.addEventListener);
safeFunction(EventTarget.prototype.removeEventListener);
safeFunction(EventTarget.prototype.dispatchEvent);

function Node() {
    EventTarget.call(this);
}

Object.setPrototypeOf(Node.prototype, EventTarget.prototype);
safeFunction(Node);

Node.prototype.appendChild = function appendChild(child) {
    v_log("DOM操作: appendChild");
    return child;
};
Node.prototype.removeChild = function removeChild(child) {
    v_log("DOM操作: removeChild");
    return child;
};
safeFunction(Node.prototype.appendChild);
safeFunction(Node.prototype.removeChild);

function Element() {
    Node.call(this);
}

Object.setPrototypeOf(Element.prototype, Node.prototype);
safeFunction(Element);

Element.prototype.getAttribute = function getAttribute(name) {
    v_log(`DOM操作: ${this.constructor.name}.getAttribute('${name}')`);
    return this[name] || null;
};
Element.prototype.setAttribute = function setAttribute(name, value) {
    v_log(`DOM操作: ${this.constructor.name}.setAttribute('${name}', '${value}')`);
    this[name] = String(value);
};
Element.prototype.removeAttribute = function removeAttribute(name) {
    v_log(`DOM操作: ${this.constructor.name}.removeAttribute('${name}')`);
    delete this[name];
};

// --- 3.4 具体的 HTML 元素构造函数扩展 ---

// 统一定义并伪装构造函数
function HTMLMetaElement() {
    HTMLElement.call(this);
}

Object.setPrototypeOf(HTMLMetaElement.prototype, HTMLElement.prototype);
Object.defineProperty(HTMLMetaElement.prototype, Symbol.toStringTag, {value: "HTMLMetaElement", configurable: true});
safeFunction(HTMLMetaElement);

function HTMLHeadElement() {
    HTMLElement.call(this);
}

Object.setPrototypeOf(HTMLHeadElement.prototype, HTMLElement.prototype);
Object.defineProperty(HTMLHeadElement.prototype, Symbol.toStringTag, {value: "HTMLHeadElement", configurable: true});
safeFunction(HTMLHeadElement);

function HTMLLinkElement() {
    HTMLElement.call(this);
}

Object.setPrototypeOf(HTMLLinkElement.prototype, HTMLElement.prototype);
Object.defineProperty(HTMLLinkElement.prototype, Symbol.toStringTag, {value: "HTMLLinkElement", configurable: true});
safeFunction(HTMLLinkElement);

// - 映射表与虚拟数据池 ---
const tagConstructorMap = {
    'meta': HTMLMetaElement,
    'head': HTMLHeadElement,
    'div': HTMLDivElement,
    'script': HTMLScriptElement,
    'link': HTMLLinkElement
};
const virtualNodes = {
    get meta() {
        return [
            {name: "referrer", content: "A2bnzJwSMjyC8NxLVUF9avD0ULH6jS59X7KfspqJdmnDFqXiu8FqI1_qwnMaYoDg"},   //
            {"http-equiv": "pragma", content: "no-cache"}
        ];
    },
    'head': [{}],
    'base': [{href: "https://www.ouyeel.com/"}],
    'link': [{rel: "stylesheet", href: "style.css"}]
};

Element.prototype.getElementsByTagName = function getElementsByTagName(tagName) {
    const tag = String(tagName).toLowerCase();
    v_log(`DOM操作: ${this.constructor.name}.getElementsByTagName('${tag}')`);

    const collection = [];

    // 如果有预设的虚拟节点，则根据数据生成实例
    if (virtualNodes[tag]) {
        virtualNodes[tag].forEach(data => {
            const Constructor = tagConstructorMap[tag] || HTMLElement;
            const el = new Constructor();
            // 填充数据
            Object.assign(el, data);
            // 必须 watch 每一个元素，使其 getAttribute 等操作被记录
            collection.push(watch(el, `Element<${tag}>`));
        });
    }

    // 设置集合标识并 watch 集合本身（处理 -1 等索引问题）
    Object.defineProperty(collection, Symbol.toStringTag, {value: "HTMLCollection"});
    return watch(collection, "HTMLCollection");
};

Element.prototype.getElementsByClassName = function getElementsByClassName(className) {
    v_log(`DOM操作: ${this.constructor.name}.getElementsByClassName('${className}')`);
    const collection = [];
    Object.defineProperty(collection, Symbol.toStringTag, {value: "HTMLCollection"});
    return watch(collection, "HTMLCollection");
};

Object.defineProperty(Element.prototype, "style", {
    get: function () {
        if (!this._style) {
            this._style = watch({}, `${this.constructor.name}.style`);
        }
        return this._style;
    },
    configurable: true,
    enumerable: true
});

Object.defineProperty(Element.prototype, "className", {
    get: function () {
        return this["class"] || "";
    },
    set: function (val) {
        this["class"] = val;
    },
    configurable: true,
    enumerable: true
});

safeFunction(Element.prototype.getAttribute);
safeFunction(Element.prototype.setAttribute);
safeFunction(Element.prototype.removeAttribute);
safeFunction(Element.prototype.getElementsByTagName);
safeFunction(Element.prototype.getElementsByClassName);

function HTMLElement() {
    Element.call(this);
}

Object.setPrototypeOf(HTMLElement.prototype, Element.prototype);
safeFunction(HTMLElement);

function HTMLDivElement() {
    HTMLElement.call(this);
}

Object.setPrototypeOf(HTMLDivElement.prototype, HTMLElement.prototype);
safeFunction(HTMLDivElement);

function HTMLScriptElement() {
    HTMLElement.call(this);
}

Object.setPrototypeOf(HTMLScriptElement.prototype, HTMLElement.prototype);
Object.defineProperty(HTMLScriptElement.prototype, Symbol.toStringTag, {
    value: "HTMLScriptElement",
    configurable: true
});
safeFunction(HTMLScriptElement);
window.HTMLScriptElement = HTMLScriptElement;


/************************************************************
 * 模块 3.5：Document (重点完善部分)
 ************************************************************/

function Document() {
    Node.call(this);
}

Object.setPrototypeOf(Document.prototype, Node.prototype);

Document.prototype.createElement = function createElement(tagName) {
    const tag = String(tagName).toLowerCase();
    v_log("DOM操作: document.createElement ->", tag);
    let element;
    switch (tag) {
        case 'div':
            element = new HTMLDivElement();
            break;
        case 'script':
            element = new HTMLScriptElement();
            break;
        default:
            element = new HTMLElement();
    }
    return watch(element, `element <${tag}>`);
};

// ⭐ 完善 getElementById
Document.prototype.getElementById = function getElementById(id) {
    v_log(`DOM操作: document.getElementById('${id}')`);
    // 创建一个真实的实例，使其自动具备 Element 的方法和 style 监控
    const element = new HTMLDivElement();
    Object.defineProperty(element, 'id', {value: id, enumerable: true});
    return watch(element, `Element(id:${id})`);
};

// ⭐ 完善 getElementsByName
Document.prototype.getElementsByName = function getElementsByName(name) {
    v_log(`DOM操作: document.getElementsByName('${name}')`);
    const collection = [];
    // 模拟集合中包含一个元素
    const element = new HTMLDivElement();
    element.name = name;
    collection.push(watch(element, `Element(name:${name})`));

    Object.defineProperty(collection, Symbol.toStringTag, {value: "NodeList"});
    return watch(collection, "NodeList");
};

Document.prototype.getElementsByTagName = Element.prototype.getElementsByTagName;
Document.prototype.getElementsByClassName = Element.prototype.getElementsByClassName;

safeFunction(Document);
safeFunction(Document.prototype.createElement);
safeFunction(Document.prototype.getElementById);
safeFunction(Document.prototype.getElementsByName);

Object.defineProperty(Document.prototype, Symbol.toStringTag, {
    value: "HTMLDocument",
    configurable: true
});

// 实例化、暴露类、多重挂载
const documentInstance = new Document();
globalThis.Document = Document;     // 暴露大写构造函数
globalThis.HTMLDocument = Document; // 兼容性别名
window.document = documentInstance;
globalThis.document = documentInstance;

// 初始化 body
document.body = watch(new HTMLDivElement(), "document.body");


/************************************************************
 * 模块 4： Navigator
 ************************************************************/
function Navigator() {
}

safeFunction(Navigator);
Object.defineProperty(Navigator.prototype, Symbol.toStringTag, {value: "Navigator", configurable: true});
Navigator.prototype.userAgent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/143.0.0.0 Safari/537.36";

const navigatorInstance = new Navigator();
window.navigator = navigatorInstance;
globalThis.navigator = navigatorInstance;
globalThis.Navigator = Navigator;

/************************************************************
 * 模块 5： Screen
 ************************************************************/
function Screen() {
}

safeFunction(Screen);
Object.defineProperty(Screen.prototype, Symbol.toStringTag, {value: "Screen", configurable: true});
// 补充常用属性
Screen.prototype.width = 1920;
Screen.prototype.height = 1080;
Screen.prototype.availWidth = 1920;
Screen.prototype.availHeight = 1040;

const screenInstance = new Screen();
window.screen = screenInstance;
globalThis.screen = screenInstance;
globalThis.Screen = Screen;

/************************************************************
 * 模块 6： History
 ************************************************************/
function History() {
}

safeFunction(History);
Object.defineProperty(History.prototype, Symbol.toStringTag, {value: "History", configurable: true});
History.prototype.length = 1;
History.prototype.state = null;

const historyInstance = new History();
window.history = historyInstance;
globalThis.history = historyInstance;
globalThis.History = History;

/************************************************************
 * 模块 7： Location
 ************************************************************/
function Location() {
    let _url = new URL("https://www.ouyeel.com/steel");

    // 将属性定义在实例上，解决 hasOwnProperty 检测
    const props = ["href", "protocol", "host", "hostname", "port", "pathname", "search", "hash", "origin"];

    props.forEach(prop => {
        Object.defineProperty(this, prop, {
            get() {
                v_log(`[Location] get ${prop}`);
                return _url[prop];
            },
            set(v) {
                if (prop === "href") {
                    try {
                        _url = new URL(String(v));
                    } catch (e) {
                    }
                } else {
                    // 模拟部分属性可写（如 location.hash）
                    try {
                        let temp = new URL(_url.href);
                        temp[prop] = String(v);
                        _url = temp;
                    } catch (e) {
                    }
                }
            },
            enumerable: true,
            configurable: false // 模拟真浏览器的不可配置特性
        });
    });

    // 屏蔽 toStringTag，让 Object.prototype.toString.call(location) 返回 [object Location]
    Object.defineProperty(this, Symbol.toStringTag, {
        value: "Location",
        configurable: true
    });
}

// 方法依然放在原型上
Location.prototype.toString = function toString() {
    return this.href;
};
safeFunction(Location.prototype.toString);

// 此时创建的实例
const _location = new Location();
// 使用你的原生方法包装逻辑
location = watch(_location, "location");

// 补一个关键点：屏蔽 Location.prototype 上的这些 get，防止脚本探测原型链
["href", "protocol", "host"].forEach(p => {
    Object.defineProperty(Location.prototype, p, {enumerable: false, value: undefined});
});

/************************************************************
 * 模块 8：挂 Proxy
 ************************************************************/

setProxyArr(["window", "document", "location", "history", "screen", "navigator"]);


```

输出 cookie 长度 304，整好和控制台输出的 cookie 长度一致，python 也能正常调用

## 加载器

loader.js

```python
metacontent = 'meta_content';
require("./01_env_gemini")  // gemini生成
// require("./01_env_copilot")  // copilot生成


setTimeout = function () {
}
setInterval = function () {
}


'js_code';
'wailian_js_code';

require("./02_code")   //使用python调用时，需注释此行


function get_cookie() {
    return document.cookie
}

console.log(get_cookie(), get_cookie().length)
```

## python 主程序

```python
import requests, execjs
from lxml import etree

session = requests.session()

# cookies = {
#     'T0k1m0u5AfREO': '5Kr8BmXYwVMluL1aO0gpc_cDiXGdFCbURnA8VZsnecdkKUUjexNAV5HpxpqCLIXDDUof6GO4POMxq.R.UhcO7rq',
#     'cookiesession1': '678A3E1A457B65ECF826195EF85DB9D7',
#     'T0k1m0u5AfREP': '257NKRclB.IdZ8daPthMu4Oo.XYTAdWKORWc.yrkhtxU1pqkAoB0qq0JxSOB0a12wnhpj4q03lieIk8bJrgr0B0iuL4Ns4iYesI5jCd_qlOn9qUtbAxHbCii5FrEin6YwMVecF5wTUJZ3rhYViQvAzsNxPVISCrj13mHcBIoMurwitNrfZphTHI.f0NpDlqxjIjQ5HIGYTZwoQ63V.SsrD1ZylTG3ZeH8dYYSU9V4JE',
# }

headers = {
    'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7',
    'Accept-Language': 'zh-CN,zh;q=0.9',
    'Cache-Control': 'no-cache',
    'Connection': 'keep-alive',
    'Pragma': 'no-cache',
    'Referer': 'https://www.ouyeel.com/steel/',
    'Sec-Fetch-Dest': 'document',
    'Sec-Fetch-Mode': 'navigate',
    'Sec-Fetch-Site': 'same-origin',
    'Upgrade-Insecure-Requests': '1',
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Safari/537.36',
    'sec-ch-ua': '"Google Chrome";v="143", "Chromium";v="143", "Not A(Brand";v="24"',
    'sec-ch-ua-mobile': '?0',
    'sec-ch-ua-platform': '"Windows"',
}

url = 'https://www.ouyeel.com/steel'     # 结尾处不能有 /
response = session.get(url, headers = headers)
response.encoding = 'utf-8'
html = etree.HTML(response.text)

meta_content = html.xpath('//meta/@content')[-1]
js_code = html.xpath('//script[1]/text()')[0]

wailian_js_code_url = "https://www.ouyeel.com" + html.xpath('//script[2]/@src')[0]  # 获取第一次请求时，页面中的外链地址
wailian_js_code = session.get(wailian_js_code_url, headers = headers).text  # 访问上一步得到的外链，获取外链的代码

# print(wailian_js_code)

# 将meta_content、js_code和wailian_js_code 替换原文件内容
with open("03_loader.js", 'r', encoding = 'utf-8') as fp:
    code = fp.read()
    code = code.replace('meta_content', meta_content).replace("'js_code'", js_code).replace("'wailian_js_code'",
                                                                                            wailian_js_code)

js_compile = execjs.compile(code)
js_cookie = js_compile.call("get_cookie")

print(js_cookie, len(js_cookie))

cookie_key_value = js_cookie.split(";")[0].split("=")
session.cookies.update({cookie_key_value[0]: cookie_key_value[1]})

response = session.get(url, headers = headers)
response.encoding = 'utf-8'
print(response.text, response.status_code)

```

测试发现，content 中的值没有用到

升级：

```python
import requests, execjs, json
from lxml import etree

session = requests.session()

# cookies = {
#     'T0k1m0u5AfREO': '5Kr8BmXYwVMluL1aO0gpc_cDiXGdFCbURnA8VZsnecdkKUUjexNAV5HpxpqCLIXDDUof6GO4POMxq.R.UhcO7rq',
#     'cookiesession1': '678A3E1A457B65ECF826195EF85DB9D7',
#     'T0k1m0u5AfREP': '257NKRclB.IdZ8daPthMu4Oo.XYTAdWKORWc.yrkhtxU1pqkAoB0qq0JxSOB0a12wnhpj4q03lieIk8bJrgr0B0iuL4Ns4iYesI5jCd_qlOn9qUtbAxHbCii5FrEin6YwMVecF5wTUJZ3rhYViQvAzsNxPVISCrj13mHcBIoMurwitNrfZphTHI.f0NpDlqxjIjQ5HIGYTZwoQ63V.SsrD1ZylTG3ZeH8dYYSU9V4JE',
# }

headers = {
    'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7',
    'Accept-Language': 'zh-CN,zh;q=0.9',
    'Cache-Control': 'no-cache',
    'Connection': 'keep-alive',
    'Pragma': 'no-cache',
    'Referer': 'https://www.ouyeel.com/steel/',
    'Sec-Fetch-Dest': 'document',
    'Sec-Fetch-Mode': 'navigate',
    'Sec-Fetch-Site': 'same-origin',
    'Upgrade-Insecure-Requests': '1',
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Safari/537.36',
    'sec-ch-ua': '"Google Chrome";v="143", "Chromium";v="143", "Not A(Brand";v="24"',
    'sec-ch-ua-mobile': '?0',
    'sec-ch-ua-platform': '"Windows"',
    # 'Cookie': 'T0k1m0u5AfREO=5Kr8BmXYwVMluL1aO0gpc_cDiXGdFCbURnA8VZsnecdkKUUjexNAV5HpxpqCLIXDDUof6GO4POMxq.R.UhcO7rq; cookiesession1=678A3E1A457B65ECF826195EF85DB9D7; T0k1m0u5AfREP=257NKRclB.IdZ8daPthMu4Oo.XYTAdWKORWc.yrkhtxU1pqkAoB0qq0JxSOB0a12wnhpj4q03lieIk8bJrgr0B0iuL4Ns4iYesI5jCd_qlOn9qUtbAxHbCii5FrEin6YwMVecF5wTUJZ3rhYViQvAzsNxPVISCrj13mHcBIoMurwitNrfZphTHI.f0NpDlqxjIjQ5HIGYTZwoQ63V.SsrD1ZylTG3ZeH8dYYSU9V4JE',
}

url = 'https://www.ouyeel.com/steel'
response = session.get(url, headers = headers)
response.encoding = 'utf-8'
html = etree.HTML(response.text)

meta_content = html.xpath('//meta/@content')[-1]
js_code = html.xpath('//script[1]/text()')[0]
wailian_js_code_url = "https://www.ouyeel.com" + html.xpath('//script[2]/@src')[0]  # 获取第一次请求时，页面中的外链地址
wailian_js_code = session.get(wailian_js_code_url, headers = headers).text  # 访问上一步得到的外链，获取外链的代码

# print(wailian_js_code)

# 将meta_content、js_code和wailian_js_code 替换03_loader.js中的变量
with open("03_loader.js", 'r', encoding = 'utf-8') as fp:
    code = fp.read()

code = code.replace('meta_content', json.dumps(meta_content))  # 03_loader中的 'meta_content' 两边不要带引号，
code = code.replace("'js_code'", js_code)
code = code.replace("'wailian_js_code'", wailian_js_code)

# 如果有必要，可以保存一个调试文件看一眼
# with open("debug_loader.js", "w", encoding = "utf-8") as f:
#     f.write(code)

js_compile = execjs.compile(code)

# 配合03_loader，检查content是否正确赋值
# debug_data = js_compile.call("get_debug_info")
# print("发起请求获取的值:", meta_content)
# print("JS 内部变量值:  ", debug_data['variable'])
# print("DOM 节点属性值: ", debug_data['dom_content'])

js_cookie = js_compile.call("get_cookie")

print(js_cookie, len(js_cookie))

cookie_key_value = js_cookie.split(";")[0].split("=")
session.cookies.update({cookie_key_value[0]: cookie_key_value[1]})

response = session.get(url, headers = headers)
response.encoding = 'utf-8'
print(response.text, response.status_code)

```
