
## 一、目的
请求地址：https://www.hfhuizhan.com/schedule#  ，获取每个月的展会信息

## 二、加密参数观察

打开页面后，切换不同月份，发现响应数据在 `listExhibitionNotPage` 中

观察 `listExhibitionNotPage`  

请求头： 请求方法是 post，没有加密参数
content-type： application/json;charset=UTF-8, application/json;charset=UTF-8
载荷：点击“查看源码，可以看到 `{data: "RDXU5j25HNePIYXSi6WP+Fm+C4LL+OkDZgy5AYaWbeo=}"`，每次切换月份，data 的值都不一样，所以 data 是需要逆向
预览/响应： 响应数据是加密的

所以本次逆向有： 请求参数中的 `data`  以及响应数据的解码

## 三、逆向


由于请求头中 content-type 是 `application/json;charset=UTF-8, application/json;charset=UTF-8`，所以参数是 json 格式，同时响应结果是加密，在解密后，往往需要使用 json.parse 进行解析。 所以使用 hook json.parse 和 json.stringify。 

于是采用油猴脚本进行 hook 拦截。

### 1、请求参数加密函数定位

加载页面后，切换月份，触发 json.stringify ，返回主程序，直接定位到

![[Pasted image 20260613225306.png]]


执行 `sr(JSON.stringify(Xe.data))`，直接得到 `'RDXU5j25HNePIYXSi6WP+IG4MYNs9g8IHg05gr9F35k='`
由此得知，sr 函数是载荷参数加密的关键

单步进入 sr 函数
```
 , Et = Ue
  , Dt = e("NFKh")
  , jt = e.n(Dt)
  , Zt = "$shanghaidianqi$"
  , Vt = "2023050814260000"
  , sr = function(Xe) {
    var ot = jt.a.enc.Utf8.parse(Zt)
      , Kt = jt.a.enc.Utf8.parse(Vt)
      , kt = jt.a.AES.encrypt(Xe, ot, {
        iv: Kt,
        mode: jt.a.mode.CBC,
        padding: jt.a.pad.Pkcs7
    });
    return kt.toString()
}
```

采用 AES加密方式，直接扔给 AI 生成 python 代码即可

### 2、响应数据解密函数定位

继续执行代码，触发 hook 脚本的 JSON.parse 拦截，返回主程序后

![[Pasted image 20260613230007.png]]

观察 `kt` 的值，发现是明文的响应数据：

![[Pasted image 20260613230058.png]]

也就是说上图中的 `kt = JSON.parse(mr(kt)),` mr 函数就是完成响应数据解密的函数

跟踪进入 mr 函数，就是 AES 的解密
```
, mr = function(Xe) {
          var ot = jt.a.enc.Utf8.parse(Zt)
            , Kt = jt.a.enc.Utf8.parse(Vt)
            , kt = jt.a.AES.decrypt(Xe, ot, {
              iv: Kt,
              mode: jt.a.mode.CBC,
              padding: jt.a.pad.Pkcs7
          })
            , yr = kt.toString(jt.a.enc.Utf8);
          return yr.toString()
      }
```

扔给 AI 生成 python 代码即可

---

## 四、补充信息（给 AI 逆向用）

  
如果下次做类似逆向，把这些信息给 AI 就能直接出 Python 代码，不需要跑浏览器抓包：


### 完整 API 信息
  
```

API 地址: POST https://www.hfhuizhan.com/prod-api/hfhz-exhibition/back/exhibition/listExhibitionNotPage

```

  
### 请求格式

- Content-Type: `application/json;charset=UTF-8`
- 请求体: `{"data": "<AES密文的Base64>"}`
- 明文参数: `{"yyyyMM": "2026-06"}` — 只有这一个字段，切换月份就改这个

### 响应格式 

- Content-Type: `application/json;charset=UTF-8`
- **响应体直接就是 Base 64 密文**（没有 `{"data": "..."}` 包装，这点和请求不一样）
- 解密后是 JSON: `{"code": 200, "data": [...]}`


### 加密算法总结

| 场景 | 方向 | 位置 | 算法 | 密钥 | IV | 填充 | 编码 |
|---|---|---|---|---|---|---|---|
| 请求参数加密 | 加密 | 请求体 `{"data": "..."}` | AES-128-CBC | `$shanghaidianqi$` | `2023050814260000` | PKCS7 | Base64 |
| 响应数据解密 | 解密 | 响应体（直接是密文） | AES-128-CBC | `$shanghaidianqi$` | `2023050814260000` | PKCS7 | Base64 |

  
### Python 实现

安装依赖：
```
uv add pycryptodome
uv add requests
```
  
加密纯算代码
```

from Crypto.Cipher import AES

from Crypto.Util.Padding import pad, unpad

import base64

  

KEY = b"$shanghaidianqi$"

IV  = b"2023050814260000"

  

def encrypt(plaintext: str) -> str:

    cipher = AES.new(KEY, AES.MODE_CBC, IV)

    ct = cipher.encrypt(pad(plaintext.encode(), AES.block_size))

    return base64.b64encode(ct).decode()

  

def decrypt(b64: str) -> str:

    cipher = AES.new(KEY, AES.MODE_CBC, IV)

    pt = unpad(cipher.decrypt(base64.b64decode(b64)), AES.block_size)

    return pt.decode()

```

主程序
```
"""

合肥滨湖国际会展中心 — 展会信息获取

逆向:
  网站: https://www.hfhuizhan.com/schedule
  加密: AES-128-CBC, key=$shanghaidianqi$, iv=2023050814260000, PKCS7

"""

import json
import requests
from crypto_utils import encrypt as encrypt_data, decrypt as decrypt_data
 

# === 修改下面这个月份即可获取对应数据 ===

MONTH = "2026-01"
API_URL = "https://www.hfhuizhan.com/prod-api/hfhz-exhibition/back/exhibition/listExhibitionNotPage"

HEADERS = {
    "Content-Type": "application/json;charset=UTF-8",
    "Accept": "application/json",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
    "Referer": "https://www.hfhuizhan.com/schedule",
}

def fetch_exhibitions(year_month: str) -> dict:

    encrypted = encrypt_data(json.dumps({"yyyyMM": year_month}))
    body = json.dumps({"data": encrypted}, ensure_ascii=False)
    resp = requests.post(API_URL, data=body, headers=HEADERS, timeout=30)
  
    # API returns raw encrypted base64 text, decrypt → JSON
    return json.loads(decrypt_data(resp.text.strip()))
  

if __name__ == "__main__":

    result = fetch_exhibitions(MONTH)
    print(f"月份: {MONTH}  共 {len(result.get('data', []))} 场展会")
    print(json.dumps(result, ensure_ascii=False, indent=2))
```


