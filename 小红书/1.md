某红薯新版xs（许久没登录了，来水一水补环境版）
这几个月很忙没上号，xs参数也不知道什么时候改的，是有人提了才来看一眼，反正是从XYW开头变成了XYS，xscommon验证了没有改，所以跳过，感兴趣的去看前面的文章。
参数名没有混淆，直接搜"X-s"就能搜出来
图片
参数名赋值给了下划线，下划线通过v(x, s)赋值，在这个位置下断点，断住后进v函数
图片
返回值打断点看了下，f转字符串，然后经过了lz、xE函数，lz是转utf8编码，xE是bs64编码。这两没什么好看的，后续直接全抠出来就行了，还有两个依赖函数也抠出来就完事了。
重点看f，f除了x3都是常规字符串，所以我们只需要知道f，也就是s = window.mnsv2(c, d)的结果即可。看到window.mnsv2还是很舒服的。这种赋值给全局变量的加密函数只需要考虑补环境即可，不用考虑怎么导出使用。
图片
c的值就是接口拼接请求参数，这个倒是没改，d是32位的看起来像md5。
验证一下，确实是
图片
window.mnsv2下断后点进去就进vm了，这种虚拟机的混淆代码没法看，直接在vm文件开头打上断点刷新页面，断住后，我们回溯堆栈，去到进入虚拟机的前一个堆栈

图片
这里发现两个eval

图片
都打上断点，然后把vm文件拷出来。就可以开始愉快的补环境啦

补环境部分繁杂且不难，所以就不水了

这里提一下坑，这里补完环境后是能出结果的，但是这个结果是不对的

图片
生成的结果有个前缀 mns0301，但并不是所有接口都是0301，也有0201,0101，而上面的代码生成的结果貌似都是0201，而主要的数据接口基本都是0301的，这就是问题所在。

一开始以为是环境补漏了，但我把代码copy到浏览器中跑一样生成了0201，这就很尴尬了。这么说不是环境的问题？

于是就考虑同样是浏览器，传入的参数一样为什么生成的结果不一样，想到的第一个可能是某些参与运算的东西导致的，毕竟访问这个网站之后它可能把页面上的某些东西改了，比如cookie之类的，然后又重头到位刷了一遍，发现改完还是不对。再往下考虑，vm源代码少了？我就在eval那个文件搜了一下，发现eval不止两个，搜出来四五个都打上断点，最后发现总共四个vm文件，于是就全搞出来，又补了几行环境，跑了一下

图片
铛铛铛铛~0301出来了。拿去请求了一下笔记详情能正确获取响应。

完事，又可以愉快地划水了~